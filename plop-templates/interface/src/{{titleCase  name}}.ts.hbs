import { LitElement, html } from "lit";
import { customElement, property } from "lit/decorators.js";
import { OslControl } from '../../Interface/src/Interface';

@customElement("osl-{{lowerCase name}}")
export class Osl{{titleCase name}} extends OslControl {
  render() {
    return html`
      <div>
        <h1>Hello, {{titleCase name}}!</h1>
      </div>
    `;
  }
}

declare global {
  interface HTMLElementTagNameMap {
    "osl-{{lowerCase name}}": Osl{{titleCase name}};
  }
}